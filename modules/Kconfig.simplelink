# CC3220/CC32XX SDK HAL configuration

config HAS_CC3220SDK
	bool

# Notes:
# SimpleLink drivers require types (stdint.h) from c library which is not
# provided by minimal lbc
# Selecting ERRNO lets host driver use Zephyr's __errno
# Selecting POSIX_THREADS and POSIX_API are needed to build the host driver
config SIMPLELINK_HOST_DRIVER
	bool "Build the SimpleLink WiFi Host Driver"
	depends on HAS_CC3220SDK
	depends on MULTITHREADING
	select REQUIRES_FULL_LIBC
	select ERRNO
	select POSIX_THREADS
	select POSIX_TIMERS
	help
	  Build the SimpleLink host driver

# MSP432 SDK HAL configuration

config HAS_MSP432P4XXSDK
	bool
	select HAS_CMSIS_CORE

# CC13X2 / CC26X2 SDK HAL configuration

config HAS_CC13X2_CC26X2_SDK
	bool

config HAS_CC13X2X7_CC26X2X7_SDK
	bool

# CC23X0 SDK HAL configuration

config HAS_CC23X0_SDK
	bool

# CC27XX SDK HAL configuration

config HAS_CC27XX_SDK
	bool

config SIMPLELINK_F3_RADIO_DRIVER
	bool "Build the RCL driver"
	depends on HAS_CC23X0_SDK || HAS_CC27XX_SDK
	select ENTROPY_GENERATOR
	default n if MCUBOOT
	default y
	help
		Build the RCL driver

config SIMPLELINK_F3_RF_SETTING_PREGEN
	bool "Use the pre-generated radio settings"
	depends on SIMPLELINK_F3_RADIO_DRIVER
	default y
	help
		Use the pre-generated radio settings

menu "Regulatory Domain Selection"
	depends on SIMPLELINK_F3_RADIO_DRIVER
	depends on SOC_CC2755P10 || SOC_CC2755R10 || SOC_CC2745R10_Q1

config RCL_REGULATORY_DOMAIN_ETSI
	bool "ESTI: European Telecommunications Standards Institute (Europe)"
	default n

config RCL_REGULATORY_DOMAIN_FCC
	bool "FCC: Federal Communications Commission (USA)"
	default n

config RCL_REGULATORY_DOMAIN_MIIT
	bool "MIIT: Ministry of Information and Communications Technology (China)"
	default n

config RCL_REGULATORY_DOMAIN_RUNTIME_MODIFIABLE
	bool "Allow runtime modification of regulatory domain mask"
	default n

endmenu

# CC35XX SDK HAL configuration
config HAS_CC35XX_SDK
	bool
