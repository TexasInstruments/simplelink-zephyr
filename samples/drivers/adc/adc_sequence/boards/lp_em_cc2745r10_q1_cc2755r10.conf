# ADC resolution for conversions.
# Supported ADC resolutions are 8, 10 and 12 bits.
# Refer to https://www.ti.com/lit/ug/swcu195a/swcu195a.pdf for more details.
CONFIG_SEQUENCE_RESOLUTION=12

# Number of samples in an ADC sequence.
CONFIG_SEQUENCE_SAMPLES=4

# Enable DMA support.
CONFIG_ADC_LPF3_DMA_DRIVEN=y

# Used only for debugging ADC DMA transactions.
CONFIG_DMA_LOG_LEVEL_DBG=y

# Need to be enabled when running GDB and also to see logs with log level DBG.
CONFIG_DEBUG=y
CONFIG_DEBUG_INFO=y

# Needs to be enabled when using GDB for debugging through application.
#CONFIG_NO_OPTIMIZATIONS=y

CONFIG_LOG=y
CONFIG_LOG_BUFFER_SIZE=4096
CONFIG_LOG_MODE_IMMEDIATE=y
CONFIG_LOG_PROCESS_THREAD=y
CONFIG_LOG_PRINTK=y
CONFIG_KERNEL_LOG_LEVEL_DBG=y
CONFIG_THREAD_NAME=y

#CONFIG_ADC_ASYNC=y
CONFIG_ADC_LOG_LEVEL_DBG=y

CONFIG_DMA_LOG_LEVEL_DBG=y

CONFIG_PM=y
CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_SYSTEM_MANAGED=y

CONFIG_IDLE_STACK_SIZE=4096
