# Copyright (c) 2026 Texas Instruments Incorporated
# Copyright (c) 2024 BayLibre, SAS
#
# SPDX-License-Identifier: Apache-2.0

description: TI SimpleLink LPF3 UART node (CC23x0, CC27xx)

compatible: "ti,lpf3-uart"

include: [uart-controller.yaml, pinctrl-device.yaml, base.yaml]

properties:
  reg:
    required: true

  interrupts:
    required: true

  pinctrl-0:
    required: true

  pinctrl-names:
    required: true

  ti,clkctl-id:
    type: int
    required: true
    description: |
      CLKCTL peripheral clock gate ID for this UART instance.
      Use CLKCTL_UART0 (0x4) for uart0, CLKCTL_UART1 (0x8) for uart1.

  dmas:
    description: |
      Optional TX & RX DMA specifiers. Each specifier will have a phandle
      reference to the DMA controller, the channel number, and the peripheral
      trigger source.

      Example for channels 2/3 with uart0txtrg/uart0rxtrg trigger sources:
        dmas = <&dma 2 6>, <&dma 3 7>;

  dma-names:
    description: |
      Required if the dmas property exists. This should be "tx" and "rx"
      to match the dmas property.

      Example:
        dma-names = "tx", "rx";
